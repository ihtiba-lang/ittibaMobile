# Ittiba

Live Quran-recitation detector for the Makkah/Madinah HLS streams (FastAPI + Whisper).

- `api.py` — the backend, deployed on a HuggingFace Space.
- `seed-tool.html` — mobile-friendly admin tool for seeding/confirming detected sessions
  (talks to the deployed API directly; open it in a browser).
